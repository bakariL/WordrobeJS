@model IEnumerable<MyWordrobeBL.Models.Sho>

@{
    ViewBag.Title = "Index";
}

<div class="row">
    <div class="col-lg-4">
        <a href="~/Home/Index"> <img src="~/Content/iconeOtherpics/icons8-back-arrow-64.png" /> <P>go back</P></a>

    </div>
    <div class="col-lg-4">
        <h2>shots</h2>
    </div>
    <div class="col-lg-4 last">
        <a href="~/Shoes/Create">
            <img src="~/Content/iconeOtherpics/icons8-add-64.png" />    <p>add new Shoes</p>
        </a>

    </div>
</div>
<div class="row">
    @foreach (var item in Model)
    {
    <div class="col-lg-pull-4">
        <div class="img-thumbnail">


            <img src="@Html.DisplayFor(modelItem => item.Photo.Photo1)" width="200" height="200" />
            <p>
                @Html.ActionLink("Edit", "Edit", new { id = item.ShoesID })
                @Html.ActionLink("Details", "Details", new { id = item.ShoesID })
                @Html.ActionLink("Delete", "Delete", new { id = item.ShoesID })
            </p>
        </div>
    </div>
    }
    </div>

</a>

