@model IEnumerable<MyWordrobeBL.Models.TopSize>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Size)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Size)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TopSizeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TopSizeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TopSizeID })
        </td>
    </tr>
}

</table>
